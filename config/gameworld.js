module.exports = {
    maxPlayers: 5, // maximum players available in the room
    tickRate: 60, // how often the server updates per second
    warningTime: 0, // duration required until server warn users about safe area (ms)
    triggerTime: 30000, // duration required until server warn users about safe area (ms)
    restrictTime: 15000, // duration required safe area to re-position and re-scale (ms)
    damageInterval: 1000,
    countdownInterval: 1000,
    NumberOfItems: 100
}